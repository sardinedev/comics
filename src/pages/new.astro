---
import Layout from "@layouts/Layout.astro";
import { getNewlyAddedIssues } from "@data/elastic/queries";
import { getWeeklyComics } from "@data/comicvine/comicvine";
import CoverLeaf from "@components/CoverLeaf/CoverLeaf.astro";
import CoverGrid from "@components/CoverGrid.astro";
import EyebrowLabel from "@components/EyebrowLabel.astro";
import PageHeading from "@components/PageHeading.astro";

const now = new Date();
const dayOfWeek = now.getDay();
const daysFromMonday = (dayOfWeek + 6) % 7;
const monday = new Date(now);
monday.setDate(now.getDate() - daysFromMonday);
monday.setHours(0, 0, 0, 0);

const sunday = new Date(monday);
sunday.setDate(monday.getDate() + 6);

const fmt = (d: Date) => d.toISOString().slice(0, 10);
const weekLabel = monday.toLocaleDateString("en-US", { month: "short", day: "numeric" })
  + " – "
  + sunday.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });

const [addedIssues, weeklyComics] = await Promise.all([
  getNewlyAddedIssues(),
  getWeeklyComics(fmt(monday), fmt(sunday)),
]);
---

<Layout title="New this week">
  <div class="mb-10 border-b border-slate-800 pb-6">
    <EyebrowLabel class="mb-1">Week of {weekLabel}</EyebrowLabel>
    <PageHeading>New This Week</PageHeading>
  </div>

  <!-- Recently added to library -->
  <section class="mb-16">
    <div class="mb-6 flex items-end justify-between">
      <div>
        <EyebrowLabel class="mb-1">In your collection</EyebrowLabel>
        <h2 class="text-2xl tracking-tight text-white">Recently Added</h2>
      </div>
      <span class="text-xs font-bold uppercase tracking-widest text-slate-500">{addedIssues.length} issue{addedIssues.length !== 1 ? "s" : ""}</span>
    </div>

    {addedIssues.length === 0 ? (
      <p class="text-sm text-slate-600">Nothing added yet.</p>
    ) : (
      <CoverGrid>
        {addedIssues.map((issue) => (
          <CoverLeaf
            type="Issue"
            id={issue.issue_id}
            cover={issue.issue_cover_url}
            number={issue.issue_number}
            seriesName={issue.series_name}
            seriesId={issue.series_id}
            badge="new"
            thumbHash={issue.issue_cover_thumb_hash}
          />
        ))}
      </CoverGrid>
    )}
  </section>

  <!-- Published this week via ComicVine -->
  <section class="mb-12">
    <div class="mb-6 flex items-end justify-between">
      <div>
        <EyebrowLabel class="mb-1">Store date {weekLabel}</EyebrowLabel>
        <h2 class="text-2xl tracking-tight text-white">Published This Week</h2>
      </div>
      <span class="text-xs font-bold uppercase tracking-widest text-slate-500">{weeklyComics.totalResults} issue{weeklyComics.totalResults !== 1 ? "s" : ""}</span>
    </div>

    {weeklyComics.issues.length === 0 ? (
      <p class="text-sm text-slate-600">No issues this week.</p>
    ) : (
      <ol class="grid grid-cols-3 gap-x-4 gap-y-8 md:grid-cols-5 lg:grid-cols-8">
        {weeklyComics.issues.map((issue) => (
          <li class="group flex flex-col gap-2">
            <a
              href={`/series/${issue.volume.id}`}
              class="relative block overflow-hidden rounded-sm ring-1 ring-white/5 transition-all duration-200 hover:ring-amber-500/40 hover:shadow-lg hover:shadow-amber-500/10"
            >
              <img
                src={issue.image.small_url}
                alt={`${issue.volume.name} #${issue.issue_number}`}
                width={210}
                height={320}
                class="aspect-[2/3] w-full object-cover transition-transform duration-300 group-hover:scale-[1.02]"
                loading="lazy"
              />
            </a>
            <div class="flex flex-col gap-0.5 px-0.5">
              <span class="truncate text-sm font-bold leading-tight text-white transition-colors group-hover:text-amber-400">{issue.volume.name}</span>
              <span class="text-xs text-slate-500">#{issue.issue_number}{issue.name ? ` — ${issue.name}` : ""}</span>
            </div>
          </li>
        ))}
      </ol>
    )}
  </section>
</Layout>
